# Examples

Reference examples for the current stack-generation model.

The production-shaped authored content now lives in the sibling
`stack-content` repo:

```text
templates/<set>/defaults.yaml
templates/<set>/configs/**
templates/<set>/environments/**
package-sets/**
package-repos/**
stacks/<stack>/stack.yaml
systems/<system>/profile.yaml
systems/<system>/deployment.yaml
```

This directory remains for planning-side reference material and future golden
fixtures. Keep examples aligned with:

- `docs/stack_generation_structure_v1.md`
- `docs/end_to_end_map_v1.md`
- `docs/deployment_inputs_and_ownership_v1.md`
